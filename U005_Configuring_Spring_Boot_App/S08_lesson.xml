<?xml version='1.0' encoding='UTF-8'?>
<slide xmlns="https://ulearn.me/schema/v2" title="Открываем доступ к Actuator" id="7ce2b67a-4f04-49ef-80cf-cb8173b5c553">
    <markdown>
        Уровень безопасности Actuator по умолчанию открывает нам очень ограниченную информацию
        из разделов `health` и `info`. А конечная точка `/actuator/health` предоставляет очень краткие сведения
        о состоянии приложения.

        Для множества приложений существуют зависимости, для которых Actuator отслеживает
        дополнительную информацию о состоянии, но не открывает к ней доступ, если на это нет
        разрешения. Чтобы посмотреть расширенную информацию о состоянии приложения для заранее
        настроенных зависимостей, и добавим в файл `application.properties` свойство:

        ```
        management.endpoint.health.show-details=always
        ```

        *Существует три возможных значения свойства-индикатора состояния приложения `show-
        details: never` (по умолчанию), `when_authorized` и `always`. В примере значение `always`, для
        демонстрации всех возможностей, но для любых приложений в продакшене следует
        выбирать варианты `never` или `when_authorized`, чтобы ограничить видимость расширенной
        информации о работоспособности приложения.*

        После перезапуска приложения при обращении к конечной точке `/actuator/ health` к общей
        сводке его состояния добавляется информация о состоянии его основных компонентов.
    </markdown>
</slide>